{# templates/footer.html.twig #}
{% block navFooter %}
<nav style="margin-top: 1rem" class="max-width navbar navbar-expand-md navbar-dark bg-dark">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsBottom" aria-controls="navbarsBottom" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-md-center" id="navbarsBottom">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_imprint') }}">{{ 'navigation.imprint'|trans({}, 'messages') }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_contact') }}">{{ 'navigation.contact'|trans({}, 'messages') }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_privacy') }}">{{ 'navigation.privacy'|trans({}, 'messages') }}</a>
            </li>
        </ul>
    </div>
    <a class="navbar-text"><a href="{{ path('change_log') }}" class="version">v1.0.12</a>
</nav>
{% endblock %}