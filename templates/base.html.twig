<!DOCTYPE html>
<html lang="de">
    <head>
        <meta charset="UTF-8">
        <link rel="shortcut icon" type="image/x-icon" href="{{ asset('build/images/nakade_256x256.png') }}">
        <title>{% block title %}Willkommen bei Nakade!{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>

    <body class="d-flex flex-column h-100">

    {{ include('nav.html.twig') }}

    <main role="main" class="flex-shrink-0" id="content">
        <div class="container">
            <h1 style="color: #C82829">Unserer Webseite ist im Aufbau!</h1>
            <img height="150px" width="150px" src="{{ asset('build/images/nakade_256x256.png') }}" alt="Symfony!"/>
            {% block body %}

            {% endblock %}
        </div>
    </main><!-- /.container -->

    <footer class="footer mt-auto py-3">
        <div class="container" style="text-align: center">
            <span class="text-muted ">UNDER CONSTRUCTION!!!</span>
        </div>
    </footer>

    {{ include('footer.html.twig') }}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>
